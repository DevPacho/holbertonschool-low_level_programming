#include <stdlib.h>
#include <stdio.h>

/**
 * main - main function (prints the name of the file it was compiled from).
 * Return: 0 (Succes).
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}

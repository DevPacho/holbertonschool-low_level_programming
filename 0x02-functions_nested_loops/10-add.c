#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: int #1 to add
 * @b: int #2 to add
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	return (a + b);
}
